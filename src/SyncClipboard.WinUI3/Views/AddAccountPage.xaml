<?xml version="1.0" encoding="utf-8" ?>
<Page x:Class="SyncClipboard.WinUI3.Views.AddAccountPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ui="using:CommunityToolkit.WinUI.Controls"
      xmlns:i18n="using:SyncClipboard.Core.I18n"
      xmlns:vm="using:SyncClipboard.Core.ViewModels"
      xmlns:vc="using:SyncClipboard.WinUI3.ValueConverters"
      MinWidth="550"
      d:DataContext="{d:DesignInstance vm:AddAccountViewModel}"
      BorderBrush="Transparent"
      mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:SettingsCard Grid.Row="0" Header="{x:Bind i18n:Strings.AccountType}">
            <ui:SettingsCard.HeaderIcon>
                <FontIcon Glyph="&#xE8C8;" />
            </ui:SettingsCard.HeaderIcon>
            <ComboBox MinWidth="200"
                      ItemsSource="{x:Bind _viewModel.LoginTypes}"
                      SelectedItem="{x:Bind _viewModel.SelectedType, Mode=TwoWay}"
                      PlaceholderText="{x:Bind i18n:Strings.SelectAccountType}" />
        </ui:SettingsCard>
        <Frame x:Name="ConfigurationFrame" Grid.Row="1" Margin="0,10,0,0" />
    </Grid>
</Page>