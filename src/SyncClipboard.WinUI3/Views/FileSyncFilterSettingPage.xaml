<?xml version="1.0" encoding="utf-8" ?>
<Page x:Class="SyncClipboard.WinUI3.Views.FileSyncFilterSettingPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ui="using:CommunityToolkit.WinUI.Controls"
      xmlns:vm="using:SyncClipboard.Core.ViewModels"
      xmlns:vc="using:SyncClipboard.WinUI3.ValueConverters"
      xmlns:i18n="using:SyncClipboard.Core.I18n"
      mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <ui:SettingsCard Grid.Row="0" Header="{x:Bind i18n:Strings.FilterMode}">
            <ui:SettingsCard.HeaderIcon>
                <FontIcon Glyph="&#xE71C;" />
            </ui:SettingsCard.HeaderIcon>
            <ComboBox SelectedItem="{x:Bind _viewModel.FilterMode, Mode=TwoWay}" ItemsSource="{x:Bind vm:FileSyncFilterSettingViewModel.Modes}" DisplayMemberPath="ShownString" />
        </ui:SettingsCard>
        <Grid Grid.Row="1" Margin="0,10,0,0" Visibility="{x:Bind vc:ConvertMethod.BoolToVisibility(_viewModel.EnableList), Mode=OneWay}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Button Grid.Row="0"
                    Margin="0,0,0,10"
                    HorizontalAlignment="Left"
                    Click="AddItemClick">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE710;" FontSize="12" />
                    <TextBlock Text="{x:Bind i18n:Strings.AddItem}" />
                </StackPanel>
            </Button>
            <ScrollViewer Grid.Row="1" VerticalScrollMode="Enabled" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <Grid Padding="12,8"
                          Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                          BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                          BorderThickness="1"
                          CornerRadius="8,8,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*" />
                            <ColumnDefinition Width="1*" />
                            <ColumnDefinition Width="80" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{x:Bind i18n:Strings.FileFilterPattern}" FontWeight="SemiBold" />
                        <TextBlock Grid.Column="1" Text="{x:Bind i18n:Strings.MatchMode}" FontWeight="SemiBold" />
                        <TextBlock Grid.Column="2"
                                   Text="{x:Bind i18n:Strings.Operation}"
                                   FontWeight="SemiBold"
                                   HorizontalAlignment="Center" />
                    </Grid>
                    <ItemsControl ItemsSource="{x:Bind _viewModel.FilterList, Mode=OneWay}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate x:DataType="vm:EditableFileFilterRule">
                                <Grid Padding="12,8"
                                      Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                      BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                      BorderThickness="1,0,1,1">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="2*" />
                                        <ColumnDefinition Width="1*" />
                                        <ColumnDefinition Width="80" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0"
                                               Text="{Binding Pattern}"
                                               TextWrapping="Wrap"
                                               VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="1"
                                               Text="{Binding MatchMode.ShownString}"
                                               TextWrapping="Wrap"
                                               VerticalAlignment="Center" />
                                    <StackPanel Grid.Column="2"
                                                Orientation="Horizontal"
                                                Spacing="4"
                                                VerticalAlignment="Center"
                                                HorizontalAlignment="Center">
                                        <Button Click="EditItemClick"
                                                DataContext="{Binding}"
                                                Width="32"
                                                Height="32"
                                                Padding="0"
                                                ToolTipService.ToolTip="{x:Bind i18n:Strings.EditRule}">
                                            <FontIcon Glyph="&#xE70F;" FontSize="14" />
                                        </Button>
                                        <Button Click="DeleteItemClick"
                                                DataContext="{Binding}"
                                                Width="32"
                                                Height="32"
                                                Padding="0"
                                                ToolTipService.ToolTip="{x:Bind i18n:Strings.DeleteRule}">
                                            <FontIcon Glyph="&#xE74D;" FontSize="14" />
                                        </Button>
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>
