<?xml version="1.0" encoding="utf-8" ?>
<Page x:Class="SyncClipboard.WinUI3.Views.ClipboardOwnerFilterSettingPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:SyncClipboard.WinUI3.Views"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ui="using:CommunityToolkit.WinUI.Controls"
      xmlns:m="using:SyncClipboard.Core.Models"
      xmlns:vm="using:SyncClipboard.Core.ViewModels"
      xmlns:vc="using:SyncClipboard.WinUI3.ValueConverters"
      xmlns:i18n="using:SyncClipboard.Core.I18n"
      mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <ui:SettingsCard Grid.Row="0" Header="{x:Bind i18n:Strings.FilterMode}" Description="{x:Bind _viewModel.Description, Mode=OneWay}">
            <ui:SettingsCard.HeaderIcon>
                <FontIcon Glyph="&#xE71C;" />
            </ui:SettingsCard.HeaderIcon>
            <ComboBox SelectedItem="{x:Bind _viewModel.FilterMode, Mode=TwoWay}" ItemsSource="{x:Bind vm:ClipboardOwnerFilterSettingViewModel.Modes}" DisplayMemberPath="ShownString" />
        </ui:SettingsCard>
        <Grid Grid.Row="1" Margin="0,10,0,0" Visibility="{x:Bind vc:ConvertMethod.BoolToVisibility(_viewModel.EnableText), Mode=OneWay}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0"
                        Orientation="Horizontal"
                        Margin="0,0,0,10"
                        Spacing="8">
                <Button Click="AddItemClick">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE710;" FontSize="12" />
                        <TextBlock Text="{x:Bind i18n:Strings.AddItem}" />
                    </StackPanel>
                </Button>
                <Button Click="CaptureClick" IsEnabled="{x:Bind vc:ConvertMethod.Not(_viewModel.IsListening), Mode=OneWay}">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE723;" FontSize="12" />
                        <TextBlock Text="{x:Bind i18n:Strings.CaptureFromClipboard}" />
                    </StackPanel>
                </Button>
                <TextBlock Text="{x:Bind i18n:Strings.ListeningNextChange}"
                           VerticalAlignment="Center"
                           Foreground="{ThemeResource SystemAccentColor}"
                           Visibility="{x:Bind vc:ConvertMethod.BoolToVisibility(_viewModel.IsListening), Mode=OneWay}" />
            </StackPanel>
            <ScrollViewer Grid.Row="1" VerticalScrollMode="Enabled" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <Grid Padding="12,8"
                          Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                          BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                          BorderThickness="1"
                          CornerRadius="8,8,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*" />
                            <ColumnDefinition Width="1*" />
                            <ColumnDefinition Width="1*" />
                            <ColumnDefinition Width="80" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{x:Bind i18n:Strings.ProcessName}" FontWeight="SemiBold" />
                        <TextBlock Grid.Column="1" Text="{x:Bind i18n:Strings.WindowTitle}" FontWeight="SemiBold" />
                        <TextBlock Grid.Column="2" Text="{x:Bind i18n:Strings.ExecutableName}" FontWeight="SemiBold" />
                        <TextBlock Grid.Column="3"
                                   Text="{x:Bind i18n:Strings.Operation}"
                                   FontWeight="SemiBold"
                                   HorizontalAlignment="Center" />
                    </Grid>
                    <ItemsControl ItemsSource="{x:Bind _viewModel.FilterList, Mode=OneWay}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate x:DataType="vm:EditableWindowInfo">
                                <Grid Padding="12,8"
                                      Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                      BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                      BorderThickness="1,0,1,1">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="1*" />
                                        <ColumnDefinition Width="1*" />
                                        <ColumnDefinition Width="1*" />
                                        <ColumnDefinition Width="80" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0"
                                               Text="{Binding ProcessName}"
                                               TextWrapping="Wrap"
                                               VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="1"
                                               Text="{Binding WindowTitle}"
                                               TextWrapping="Wrap"
                                               VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="2"
                                               Text="{Binding ExecutableName}"
                                               TextWrapping="Wrap"
                                               VerticalAlignment="Center" />
                                    <StackPanel Grid.Column="3"
                                                Orientation="Horizontal"
                                                Spacing="4"
                                                VerticalAlignment="Center"
                                                HorizontalAlignment="Center">
                                        <Button Click="EditItemClick"
                                                DataContext="{Binding}"
                                                Width="32"
                                                Height="32"
                                                Padding="0">
                                            <FontIcon Glyph="&#xE70F;" FontSize="14" />
                                        </Button>
                                        <Button Click="DeleteItemClick"
                                                DataContext="{Binding}"
                                                Width="32"
                                                Height="32"
                                                Padding="0">
                                            <FontIcon Glyph="&#xE74D;" FontSize="14" />
                                        </Button>
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>
