<?xml version="1.0" encoding="utf-8" ?>
<Page x:Class="SyncClipboard.WinUI3.Views.HistorySettingPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:SyncClipboard.WinUI3.Views"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ui="using:CommunityToolkit.WinUI.Controls"
      xmlns:i18n="using:SyncClipboard.Core.I18n"
      mc:Ignorable="d">
    <StackPanel Spacing="10">
        <ui:SettingsExpander Header="History" IsExpanded="True">
            <ui:SettingsExpander.HeaderIcon>
                <FontIcon Glyph="&#xE1D3;" />
            </ui:SettingsExpander.HeaderIcon>
            <ToggleSwitch OnContent="{x:Bind i18n:Strings.On}" OffContent="{x:Bind i18n:Strings.Off}" IsOn="{x:Bind _viewModel.EnableHistory, Mode=TwoWay}" />
            <ui:SettingsExpander.Items>
                <ui:SettingsCard Header="Close history window when losting focus">
                    <ToggleSwitch OnContent="{x:Bind i18n:Strings.On}" OffContent="{x:Bind i18n:Strings.Off}" IsOn="{x:Bind _viewModel.CloseWhenLostFocus, Mode=TwoWay}" />
                </ui:SettingsCard>
                <ui:SettingsCard Header="Max Item Count">
                    <NumberBox Minimum="0" Maximum="1000" Value="{x:Bind _viewModel.MaxItemCount, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
                </ui:SettingsCard>
            </ui:SettingsExpander.Items>
        </ui:SettingsExpander>
    </StackPanel>
</Page>
